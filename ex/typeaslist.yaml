jSchema:
  $id: https://example.com/typeaslist.schema.json
  $schema: http://json-schema.org/draft-07/schema#
  title: schema avec un type comme liste
  type: object
  properties:
    data:
      type: array
      items:
        type: [string, number]
data:
  - Yes
  - 42
  - { "Not a": "string or number" }
