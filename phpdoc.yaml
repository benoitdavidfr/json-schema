title: schema - validation de la conformité d'une instance à un schéma JSON
path: /schema
doc: |
  Utilisation de schema JSON
  https://json-schema.org/
  exemples dans ex et geojson
  
  A FAiRE:
    - définir une méthode statique jSchemaCheck() pour tester un doc par rapport à son jSchema
    - définir une méthode statique simpleCheck() pour tester un doc sans avoir à créer d'objet intermédiaire
    - réfléchir à une logique de registre de schéma:
      - URI https://schema.georef.eu/
      - définir une liste de schéma bien connus que je sais résoudre sans accès http
journal: |
  1-14/1/2019:
    améliorations de jsonschema.inc.php et définition d'une suite de tests
  31/12/2018:
    création de ce projet
phpScripts:
  - /schema/index.php
  - /schema/tests.php
phpIncludes:
  - /schema/jsonschema.inc.php
htmlFiles:
  - /schema/json-schema.schema.yaml
  - /schema/tests.yaml
  - /schema/testsformat.yaml
  - /schema/tests2.yaml
